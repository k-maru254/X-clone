import React from 'react';

function Header() {
    return (
        <header className="bg-info flex-grow-1" style={ { maxWidth: "290px" } }>
            This is the header
        </header>
    );
}

export default Header;
